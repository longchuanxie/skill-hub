# Storage Configuration
# Local storage path (relative to project root or absolute path)
STORAGE_LOCAL_PATH=uploads
# Base URL for file access (e.g., http://localhost:3000 or https://your-cdn.com)
# If empty, defaults to /uploads relative path
STORAGE_BASE_URL=
